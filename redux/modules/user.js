// Imports

// Actions

// Action Creators

// API Actions

// Initial State
const initialState = {
  isLoggedIn: false
};

// Reducer

function reducer(state = initialState, action) {
  switch (action.type) {
    // case typeName:
    //   return { ...state };

    default:
      return state;
  }
}

// Reducer Functions

// Export

// Default Reducer Export

export default reducer;
